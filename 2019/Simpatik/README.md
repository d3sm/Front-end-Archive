# prize-test
It's a tesk task done using HTML/CSS(Bootstrap) and some JS.
## Getting started - copy the project
### via https:
 <pre><code>$ git clone https://github.com/d3sm/prize-landing.git </pre></code>

### via ssh:
<pre><code>git@github.com:d3sm/prize-landing.git</pre></code>
</br> or simply download an archive.

## Check it out!